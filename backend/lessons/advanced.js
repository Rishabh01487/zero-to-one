const advancedLessons = [
  {
    id: 'lesson-51',
    title: 'DP Basics (Memoization vs Tabulation)',
    category: 'dp',
    order_index: 51,
    content: '## What is Dynamic Programming?\n\nDynamic Programming (DP) is a technique for solving problems with **overlapping subproblems** and **optimal substructure**. Instead of solving the same subproblem repeatedly, we store results and reuse them.\n\n## Two Key Properties\n\n1. **Optimal Substructure**: Optimal solution can be built from optimal solutions of subproblems\n2. **Overlapping Subproblems**: Same subproblems are solved multiple times\n\n## Top-Down (Memoization)\n\nRecursion + caching. Start from the main problem, break it down, and cache results:\n\n```cpp\nvector<int> memo(31, -1);\n\nint fib(int n) {\n    if (n <= 1) return n;\n    if (memo[n] != -1) return memo[n];  // Already computed?\n    memo[n] = fib(n-1) + fib(n-2);      // Compute and cache\n    return memo[n];\n}\n```\n\n### Without memoization:\n```\nfib(5) → fib(4) + fib(3)\n       → (fib(3) + fib(2)) + (fib(2) + fib(1))\n       → ... exponential!\n```\n\n### With memoization: each value computed once → O(n)\n\n## Bottom-Up (Tabulation)\n\nIterative, starting from the smallest subproblems and building up:\n\n```cpp\nint fib(int n) {\n    if (n <= 1) return n;\n    vector<int> dp(n + 1);\n    dp[0] = 0;\n    dp[1] = 1;\n    for (int i = 2; i <= n; i++)\n        dp[i] = dp[i-1] + dp[i-2];\n    return dp[n];\n}\n```\n\nCan optimize space further:\n```cpp\nint fib(int n) {\n    if (n <= 1) return n;\n    int prev2 = 0, prev1 = 1, curr;\n    for (int i = 2; i <= n; i++) {\n        curr = prev1 + prev2;\n        prev2 = prev1;\n        prev1 = curr;\n    }\n    return curr;\n}\n```\n\n## Memoization vs Tabulation\n\n| Aspect | Memoization (Top-Down) | Tabulation (Bottom-Up) |\n|--------|----------------------|----------------------|\n| Approach | Recursive | Iterative |\n| State | Cache + recursion | Table + loops |\n| Performance | Slightly slower (recursion overhead) | Faster (no recursion) |\n| Space | Might compute fewer states | Computes all states |\n| Implementation | Often easier (natural recursion) | More control |\n| Stack overflow risk | Yes (deep recursion) | No |\n\n## DP Framework\n\n1. **Define state**: What does `dp[i]` (or `dp[i][j]`) represent?\n2. **Recurrence**: How does `dp[i]` relate to smaller subproblems?\n3. **Base cases**: What are the simplest inputs?\n4. **Answer**: Which state contains the final answer?\n\n## When to Use DP\n\n- Problem asks for "minimum/maximum" or "number of ways"\n- Problem can be broken into smaller subproblems\n- Subproblems overlap (not divide-and-conquer like merge sort)\n- Decision at each step affects future decisions\n\n## Common DP Categories\n\n1. **1D DP**: Fibonacci, Climbing Stairs, House Robber\n2. **2D DP**: Knapsack, LCS, Edit Distance\n3. **Tree DP**: Diameter of tree, max path sum\n4. **Interval DP**: Matrix chain multiplication\n5. **DP on Grid**: Robot paths, min path sum',
    code_example: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// Top-down with memoization\nint fibMemo(int n, vector<int>& memo) {\n    if (n <= 1) return n;\n    if (memo[n] != -1) return memo[n];\n    return memo[n] = fibMemo(n-1, memo) + fibMemo(n-2, memo);\n}\n\n// Bottom-up with space optimization\nint fibTab(int n) {\n    if (n <= 1) return n;\n    int prev2 = 0, prev1 = 1, curr;\n    for (int i = 2; i <= n; i++) {\n        curr = prev1 + prev2;\n        prev2 = prev1;\n        prev1 = curr;\n    }\n    return curr;\n}\n\nint main() {\n    int n = 40;\n\n    vector<int> memo(n + 1, -1);\n    cout << \"fibMemo(\" << n << \") = \" << fibMemo(n, memo) << endl;\n    cout << \"fibTab(\" << n << \") = \" << fibTab(n) << endl;\n\n    // Compare with naive recursion (would be very slow for n=40)\n    return 0;\n}'
  },
  {
    id: 'lesson-52',
    title: 'Classic DP Problems',
    category: 'dp',
    order_index: 52,
    content: '## Essential DP Problems\n\nThese problems appear frequently in interviews and build intuition for DP patterns.\n\n## 1. 0/1 Knapsack\n\nGiven weights and values of n items, put them in a knapsack of capacity W to maximize value. Each item can be used at most once.\n\n**State**: `dp[i][w]` = max value using first i items with capacity w\n**Recurrence**:\n```\ndp[i][w] = max(dp[i-1][w], dp[i-1][w-wt[i-1]] + val[i-1]) if w >= wt[i-1]\ndp[i][w] = dp[i-1][w]  (otherwise)\n```\n\n```cpp\nint knapsack(vector<int>& val, vector<int>& wt, int W) {\n    int n = val.size();\n    vector<vector<int>> dp(n + 1, vector<int>(W + 1, 0));\n\n    for (int i = 1; i <= n; i++) {\n        for (int w = 1; w <= W; w++) {\n            if (wt[i-1] <= w) {\n                dp[i][w] = max(dp[i-1][w],\n                              dp[i-1][w - wt[i-1]] + val[i-1]);\n            } else {\n                dp[i][w] = dp[i-1][w];\n            }\n        }\n    }\n    return dp[n][W];\n}\n```\n\n**Space-optimized** (1D array):\n```cpp\nvector<int> dp(W + 1, 0);\nfor (int i = 0; i < n; i++)\n    for (int w = W; w >= wt[i]; w--)  // Reverse order!\n        dp[w] = max(dp[w], dp[w - wt[i]] + val[i]);\n```\n\n## 2. Longest Common Subsequence (LCS)\n\nFind the longest subsequence common to two strings.\n\n**State**: `dp[i][j]` = LCS of first i chars of s1 and first j chars of s2\n\n```cpp\nint LCS(string& a, string& b) {\n    int n = a.size(), m = b.size();\n    vector<vector<int>> dp(n+1, vector<int>(m+1));\n\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= m; j++) {\n            if (a[i-1] == b[j-1])\n                dp[i][j] = dp[i-1][j-1] + 1;\n            else\n                dp[i][j] = max(dp[i-1][j], dp[i][j-1]);\n        }\n    }\n    return dp[n][m];\n}\n```\n\n## 3. Longest Increasing Subsequence (LIS)\n\n**State**: `dp[i]` = length of LIS ending at index i\n\n```cpp\nint LIS(vector<int>& nums) {\n    int n = nums.size(), ans = 1;\n    vector<int> dp(n, 1);\n    for (int i = 1; i < n; i++) {\n        for (int j = 0; j < i; j++) {\n            if (nums[j] < nums[i])\n                dp[i] = max(dp[i], dp[j] + 1);\n        }\n        ans = max(ans, dp[i]);\n    }\n    return ans;\n}\n```\n\nO(n²). Can be optimized to O(n log n) using patience sorting.\n\n## 4. Coin Change (Minimum Coins)\n\n**State**: `dp[i]` = minimum coins to make amount i\n\n```cpp\nint coinChange(vector<int>& coins, int amount) {\n    vector<int> dp(amount + 1, INT_MAX);\n    dp[0] = 0;\n    for (int i = 1; i <= amount; i++) {\n        for (int coin : coins) {\n            if (i >= coin && dp[i - coin] != INT_MAX)\n                dp[i] = min(dp[i], dp[i - coin] + 1);\n        }\n    }\n    return dp[amount] == INT_MAX ? -1 : dp[amount];\n}\n```\n\n## DP Patterns Summary\n\n| Problem | State | Time | Space |\n|---------|-------|------|-------|\n| Knapsack | dp[i][w] | O(nW) | O(W) |\n| LCS | dp[i][j] | O(nm) | O(nm) |\n| LIS | dp[i] | O(n²) | O(n) |\n| Coin Change | dp[i] | O(n×amount) | O(amount) |\n| Edit Distance | dp[i][j] | O(nm) | O(nm) |',
    code_example: '#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\nusing namespace std;\n\nint LCS(string a, string b) {\n    int n = a.size(), m = b.size();\n    vector<vector<int>> dp(n+1, vector<int>(m+1));\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= m; j++) {\n            if (a[i-1] == b[j-1]) dp[i][j] = dp[i-1][j-1] + 1;\n            else dp[i][j] = max(dp[i-1][j], dp[i][j-1]);\n        }\n    }\n\n    // Reconstruct the LCS\n    string lcs;\n    int i = n, j = m;\n    while (i > 0 && j > 0) {\n        if (a[i-1] == b[j-1]) { lcs = a[i-1] + lcs; i--; j--; }\n        else if (dp[i-1][j] > dp[i][j-1]) i--;\n        else j--;\n    }\n    cout << "LCS: \" << lcs << endl;\n    return dp[n][m];\n}\n\nint main() {\n    string a = "ABCDGH", b = "AEDFHR";\n    int len = LCS(a, b);\n    cout << "Length of LCS: \" << len << endl;\n    return 0;\n}'
  },
  {
    id: 'lesson-53',
    title: 'Graphs & BFS/DFS',
    category: 'dp',
    order_index: 53,
    content: '## What is a Graph?\n\nA graph is a collection of nodes (vertices) connected by edges. Graphs model relationships: social networks, maps, web pages, dependencies.\n\n## Graph Representations\n\n### Adjacency Matrix\n`bool adj[n][n]` — O(1) edge lookup, O(n²) space.\n\n### Adjacency List (Most Common)\n```cpp\nvector<vector<int>> graph(n);  // For each vertex, list of neighbors\ngraph[0].push_back(1);  // Edge 0→1\ngraph[1].push_back(2);  // Edge 1→2\n```\nO(n + e) space, where e = number of edges.\n\n### Edge List\n```cpp\nvector<pair<int,int>> edges;\nedges.push_back({0, 1});\n```\n\n## Depth-First Search (DFS)\n\nExplores as far as possible before backtracking. Uses stack (implicit via recursion or explicit).\n\n```cpp\nvoid dfs(int u, vector<vector<int>>& graph, vector<bool>& visited) {\n    visited[u] = true;\n    cout << u << \" \";\n\n    for (int v : graph[u]) {\n        if (!visited[v]) dfs(v, graph, visited);\n    }\n}\n\n// Example: Traverse entire graph\nvector<bool> visited(n, false);\nfor (int i = 0; i < n; i++) {\n    if (!visited[i]) dfs(i, graph, visited);\n}\n```\n\n**Applications**:\n- Connected components\n- Cycle detection\n- Topological sorting\n- Maze solving\n\n## Breadth-First Search (BFS)\n\nExplores level by level. Uses queue. Finds shortest path in unweighted graph.\n\n```cpp\nvoid bfs(int start, vector<vector<int>>& graph) {\n    vector<bool> visited(graph.size(), false);\n    queue<int> q;\n\n    visited[start] = true;\n    q.push(start);\n\n    while (!q.empty()) {\n        int u = q.front(); q.pop();\n        cout << u << \" \";\n\n        for (int v : graph[u]) {\n            if (!visited[v]) {\n                visited[v] = true;\n                q.push(v);\n            }\n        }\n    }\n}\n```\n\n**Applications**:\n- Shortest path in unweighted graphs\n- Web crawling\n- Social network connections (degrees of separation)\n- Finding connected components\n\n## DFS vs BFS\n\n| Aspect | DFS | BFS |\n|--------|-----|-----|\n| Data structure | Stack (recursion) | Queue |\n| Space | O(h) depth | O(w) width |\n| Shortest path? | No (not guaranteed) | Yes (unweighted) |\n| Best for | Deep graphs, backtracking | Level-order, shortest path |\n| Cycle detection | Yes | Yes |\n\n## Graph Traversal Template\n\n```cpp\n// Generic traversal for connected components\nvoid explore(int start, vector<vector<int>>& graph, vector<bool>& visited) {\n    // Choose stack (DFS) or queue (BFS)\n    // Mark visited when processing\n    // For each neighbor, if not visited, mark and add to container\n}\n\nint countComponents(vector<vector<int>>& graph) {\n    int n = graph.size(), count = 0;\n    vector<bool> visited(n, false);\n    for (int i = 0; i < n; i++) {\n        if (!visited[i]) {\n            explore(i, graph, visited);\n            count++;\n        }\n    }\n    return count;\n}\n```\n\n## Common Graph Algorithms\n\n1. **Cycle detection**: DFS with recursion stack tracking\n2. **Topological sort**: DFS with finishing times (DAGs)\n3. **Bipartite check**: BFS coloring alternating colors\n4. **Connected components**: DFS/BFS counting components',
    code_example: '#include <iostream>\n#include <vector>\n#include <queue>\n#include <stack>\nusing namespace std;\n\nvoid addEdge(vector<vector<int>>& graph, int u, int v) {\n    graph[u].push_back(v);\n    graph[v].push_back(u);  // Undirected\n}\n\nvoid dfs(int start, vector<vector<int>>& graph, vector<bool>& visited) {\n    stack<int> s; s.push(start);\n    while (!s.empty()) {\n        int u = s.top(); s.pop();\n        if (visited[u]) continue;\n        visited[u] = true;\n        cout << u << \" \";\n        for (int v : graph[u])\n            if (!visited[v]) s.push(v);\n    }\n}\n\nvoid bfs(int start, vector<vector<int>>& graph, vector<bool>& visited) {\n    queue<int> q; q.push(start); visited[start] = true;\n    while (!q.empty()) {\n        int u = q.front(); q.pop();\n        cout << u << \" \";\n        for (int v : graph[u])\n            if (!visited[v]) { visited[v] = true; q.push(v); }\n    }\n}\n\nint main() {\n    int n = 6;\n    vector<vector<int>> graph(n);\n    addEdge(graph, 0, 1); addEdge(graph, 0, 2);\n    addEdge(graph, 1, 3); addEdge(graph, 2, 4);\n    addEdge(graph, 3, 5); addEdge(graph, 4, 5);\n\n    vector<bool> vis(n, false);\n    cout << \"DFS: \"; dfs(0, graph, vis); cout << endl;\n\n    fill(vis.begin(), vis.end(), false);\n    cout << \"BFS: \"; bfs(0, graph, vis); cout << endl;\n\n    return 0;\n}'
  },
  {
    id: 'lesson-54',
    title: 'Graph Algorithms (Dijkstra, Topological Sort)',
    category: 'dp',
    order_index: 54,
    content: '## Advanced Graph Algorithms\n\nBuilding on BFS/DFS, these algorithms solve practical graph problems.\n\n## 1. Dijkstra\'s Algorithm (Shortest Path)\n\nFinds shortest path from a source to ALL vertices in a weighted graph with non-negative weights.\n\nUses a priority queue (min-heap) to always process the nearest unvisited vertex.\n\n```cpp\nvector<int> dijkstra(vector<vector<pair<int,int>>>& graph, int src) {\n    int n = graph.size();\n    vector<int> dist(n, INT_MAX);\n    priority_queue<pair<int,int>, vector<pair<int,int>>, greater<>> pq;\n\n    dist[src] = 0;\n    pq.push({0, src});\n\n    while (!pq.empty()) {\n        int d = pq.top().first;\n        int u = pq.top().second;\n        pq.pop();\n\n        if (d > dist[u]) continue;  // Stale entry\n\n        for (auto [v, w] : graph[u]) {\n            if (dist[u] + w < dist[v]) {\n                dist[v] = dist[u] + w;\n                pq.push({dist[v], v});\n            }\n        }\n    }\n    return dist;\n}\n```\n\n**Complexity**: O((n + e) log n) with binary heap.\n\n## 2. Topological Sort\n\nOrders vertices of a Directed Acyclic Graph (DAG) so that for every edge u→v, u comes before v.\n\n### Kahn\'s Algorithm (BFS-based):\n\n```cpp\nvector<int> topologicalSort(vector<vector<int>>& graph) {\n    int n = graph.size();\n    vector<int> inDegree(n, 0);\n    for (int u = 0; u < n; u++)\n        for (int v : graph[u]) inDegree[v]++;\n\n    queue<int> q;\n    for (int i = 0; i < n; i++)\n        if (inDegree[i] == 0) q.push(i);\n\n    vector<int> result;\n    while (!q.empty()) {\n        int u = q.front(); q.pop();\n        result.push_back(u);\n        for (int v : graph[u]) {\n            if (--inDegree[v] == 0) q.push(v);\n        }\n    }\n\n    if (result.size() != n) {\n        // Graph has a cycle!\n        return {};\n    }\n    return result;\n}\n```\n\n**Applications**: Build systems (make), course prerequisites, dependency resolution.\n\n## 3. Bellman-Ford Algorithm\n\nLike Dijkstra but handles **negative edge weights**. Detects negative cycles.\n\n```cpp\nvector<int> bellmanFord(vector<vector<int>>& edges, int n, int src) {\n    vector<int> dist(n, INT_MAX);\n    dist[src] = 0;\n\n    // Relax all edges n-1 times\n    for (int i = 0; i < n - 1; i++) {\n        for (auto& e : edges) {\n            int u = e[0], v = e[1], w = e[2];\n            if (dist[u] != INT_MAX && dist[u] + w < dist[v])\n                dist[v] = dist[u] + w;\n        }\n    }\n\n    // Check for negative cycles\n    for (auto& e : edges) {\n        int u = e[0], v = e[1], w = e[2];\n        if (dist[u] != INT_MAX && dist[u] + w < dist[v])\n            return {};  // Negative cycle detected!\n    }\n    return dist;\n}\n```\n\nO(n × e) time.\n\n## 4. Floyd-Warshall (All Pairs Shortest Path)\n\n```cpp\nvector<vector<int>> floydWarshall(vector<vector<int>>& graph) {\n    int n = graph.size();\n    vector<vector<int>> dist = graph;  // Initialize with edge weights\n\n    for (int k = 0; k < n; k++)\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < n; j++)\n                if (dist[i][k] != INT_MAX && dist[k][j] != INT_MAX)\n                    dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);\n    return dist;\n}\n```\n\nO(n³) time, O(n²) space.\n\n## Algorithm Selection\n\n| Problem | Algorithm |\n|---------|-----------|\n| Single source, positive weights | Dijkstra |\n| Single source, negative weights | Bellman-Ford |\n| All pairs shortest | Floyd-Warshall |\n| DAG linear ordering | Topological sort |\n| Minimum spanning tree | Kruskal/Prim |',
    code_example: '#include <iostream>\n#include <vector>\n#include <queue>\n#include <climits>\nusing namespace std;\n\nvector<int> topologicalSort(vector<vector<int>>& graph) {\n    int n = graph.size();\n    vector<int> inDeg(n, 0);\n    for (auto& adj : graph) for (int v : adj) inDeg[v]++;\n    queue<int> q;\n    for (int i = 0; i < n; i++) if (inDeg[i] == 0) q.push(i);\n    vector<int> result;\n    while (!q.empty()) {\n        int u = q.front(); q.pop();\n        result.push_back(u);\n        for (int v : graph[u]) if (--inDeg[v] == 0) q.push(v);\n    }\n    return result;\n}\n\nint main() {\n    int n = 6;\n    vector<vector<int>> graph(n);\n    // DAG: 5→2, 5→0, 4→0, 4→1, 2→3, 3→1\n    graph[5].push_back(2); graph[5].push_back(0);\n    graph[4].push_back(0); graph[4].push_back(1);\n    graph[2].push_back(3); graph[3].push_back(1);\n\n    vector<int> order = topologicalSort(graph);\n    cout << "Topological order: \";\n    for (int v : order) cout << v << \" \";\n    cout << endl;\n\n    return 0;\n}'
  },
  {
    id: 'lesson-55',
    title: 'Hashing & Hash Tables',
    category: 'advanced',
    order_index: 55,
    content: '## What is Hashing?\n\nHashing is a technique that maps data of arbitrary size to fixed-size values (hash codes). It enables O(1) average-time lookup, insertion, and deletion.\n\n## Hash Functions\n\nA good hash function:\n1. **Deterministic**: Same input → same output\n2. **Fast to compute**\n3. **Uniform distribution**: Minimizes collisions\n4. **Avalanche effect**: Small input change → large output change\n\n### Simple Integer Hash\n\n```cpp\nsize_t hash(int key) {\n    return key % TABLE_SIZE;\n}\n```\n\n### String Hash (Polynomial Rolling Hash)\n\n```cpp\nsize_t hash(const string& s) {\n    size_t h = 0;\n    for (char c : s)\n        h = h * 31 + c;  // Common multiplier\n    return h;\n}\n```\n\n## Collision Resolution\n\nEven with good hash functions, collisions occur (two keys mapping to same bucket).\n\n### 1. Chaining\n\nEach bucket holds a linked list of key-value pairs:\n\n```cpp\nstruct HashNode {\n    int key;\n    int value;\n    HashNode* next;\n    HashNode(int k, int v) : key(k), value(v), next(nullptr) {}\n};\n\nclass HashMap {\n    vector<HashNode*> buckets;\n    int capacity;\n\n    int getBucketIndex(int key) { return key % capacity; }\n\npublic:\n    HashMap(int cap) : capacity(cap), buckets(cap, nullptr) {}\n\n    void put(int key, int value) {\n        int idx = getBucketIndex(key);\n        HashNode* node = buckets[idx];\n        while (node) {\n            if (node->key == key) { node->value = value; return; }\n            node = node->next;\n        }\n        HashNode* newNode = new HashNode(key, value);\n        newNode->next = buckets[idx];\n        buckets[idx] = newNode;\n    }\n\n    int get(int key) {\n        int idx = getBucketIndex(key);\n        HashNode* node = buckets[idx];\n        while (node) {\n            if (node->key == key) return node->value;\n            node = node->next;\n        }\n        return -1;  // Not found\n    }\n};\n```\n\n### 2. Open Addressing\n\nStore all entries in the array itself. On collision, probe for next empty slot.\n\n**Linear probing**: `(hash(key) + i) % table_size`\n**Quadratic probing**: `(hash(key) + i²) % table_size`\n**Double hashing**: `(hash1(key) + i × hash2(key)) % table_size`\n\n## Load Factor & Rehashing\n\n- **Load factor** = `n / capacity` (entries per bucket)\n- When load factor exceeds threshold (e.g., 0.75), **rehash**: double capacity, recompute all indices\n\n```cpp\nvoid rehash() {\n    capacity *= 2;\n    vector<HashNode*> oldBuckets = buckets;\n    buckets = vector<HashNode*>(capacity, nullptr);\n    for (auto& head : oldBuckets) {\n        HashNode* node = head;\n        while (node) {\n            put(node->key, node->value);\n            node = node->next;\n        }\n    }\n}\n```\n\n## std::unordered_map\n\nC++ provides hash tables via:\n\n```cpp\nunordered_map<string, int> map;\nmap[\"key\"] = 42;\nmap.insert({\"another\", 99});\nif (map.count(\"key\")) { }\nauto it = map.find(\"key\");\n```\n\nInternally uses chaining. Load factor controlled by `max_load_factor()`.\n\n## Applications\n\n1. **Caches**: Store computed results (memoization)\n2. **Databases**: Indexing for fast lookups\n3. **Symbol tables**: Compiler variable resolution\n4. **Bloom filters**: Space-efficient membership testing\n5. **Count-Min Sketch**: Frequency estimation\n\n## Best Practices\n1. Prefer `std::unordered_map` / `std::unordered_set` unless ordered iteration is needed\n2. Custom types need hash function + equality operator\n3. Avoid using keys that change hash value after insertion\n4. Choose initial capacity wisely to minimize rehashing',
    code_example: '#include <iostream>\n#include <unordered_map>\n#include <string>\n#include <vector>\nusing namespace std;\n\nvector<int> twoSum(const vector<int>& nums, int target) {\n    unordered_map<int, int> seen;  // value → index\n    for (int i = 0; i < nums.size(); i++) {\n        int complement = target - nums[i];\n        if (seen.count(complement)) {\n            return {seen[complement], i};\n        }\n        seen[nums[i]] = i;\n    }\n    return {};\n}\n\nint main() {\n    vector<int> nums = {2, 7, 11, 15};\n    int target = 9;\n    auto result = twoSum(nums, target);\n    cout << "Indices: \" << result[0] << \", \" << result[1] << endl;\n\n    // Frequency counter\n    string text = \"the quick brown fox jumps over the lazy dog\";\n    unordered_map<string, int> freq;\n    string word;\n    for (char c : text) {\n        if (c == \' \') { if (!word.empty()) { freq[word]++; word.clear(); } }\n        else word += c;\n    }\n    if (!word.empty()) freq[word]++;\n    for (auto& [w, c] : freq) cout << w << \": \" << c << endl;\n\n    return 0;\n}'
  },
  {
    id: 'lesson-56',
    title: 'Bit Manipulation & Advanced Topics',
    category: 'advanced',
    order_index: 56,
    content: '## Bit Manipulation\n\nBit manipulation operates directly on the binary representation of integers. It\'s extremely fast and useful for optimization and certain problems.\n\n## Bitwise Operators\n\n| Operator | Name | Example |\n|----------|------|---------|\n| `&` | AND | `5 & 3 = 1` (101 & 011 = 001) |\n| `|` | OR | `5 | 3 = 7` (101 | 011 = 111) |\n| `^` | XOR | `5 ^ 3 = 6` (101 ^ 011 = 110) |\n| `~` | NOT | `~5 = -6` (inverts all bits) |\n| `<<` | Left shift | `5 << 1 = 10` (multiply by 2) |\n| `>>` | Right shift | `5 >> 1 = 2` (divide by 2) |\n\n## Essential Bit Tricks\n\n### Check if k-th bit is set\n```cpp\nbool isSet(int n, int k) { return (n >> k) & 1; }\n```\n\n### Set k-th bit\n```cpp\nint setBit(int n, int k) { return n | (1 << k); }\n```\n\n### Clear k-th bit\n```cpp\nint clearBit(int n, int k) { return n & ~(1 << k); }\n```\n\n### Toggle k-th bit\n```cpp\nint toggleBit(int n, int k) { return n ^ (1 << k); }\n```\n\n### Check if power of 2\n```cpp\nbool isPowerOfTwo(int n) { return n > 0 && (n & (n - 1)) == 0; }\n```\n\n### Count set bits (Brian Kernighan)\n```cpp\nint countBits(int n) {\n    int count = 0;\n    while (n) { n = n & (n - 1); count++; }\n    return count;\n}\n```\n\n### Isolate rightmost set bit\n```cpp\nint rightmostSetBit(int n) { return n & (-n); }\n```\n\n## Memory Layout & Alignment\n\n```cpp\nstruct Example {\n    char c;     // 1 byte + 3 padding\n    int i;      // 4 bytes\n    short s;    // 2 bytes + 2 padding\n};  // Total: 12 bytes (not 7!)\n\n// Packed (no padding)\n#pragma pack(push, 1)\nstruct PackedExample {\n    char c;     // 1 byte\n    int i;      // 4 bytes\n    short s;    // 2 bytes\n};  // Total: 7 bytes\n#pragma pack(pop)\n```\n\n## Endianness\n\n- **Little-endian** (x86): Least significant byte first\n- **Big-endian**: Most significant byte first\n\n```cpp\nint x = 0x12345678;\nchar* p = (char*)&x;\n// Little-endian: 78 56 34 12\n// Big-endian:    12 34 56 78\n```\n\n## Move Semantics & Perfect Forwarding\n\n### std::move\nConverts lvalue to rvalue reference, enabling move instead of copy:\n```cpp\nvector<int> v1 = {1, 2, 3};\nvector<int> v2 = move(v1);  // v1 is now empty, v2 owns the data\n```\n\n### std::forward\nPreserves value category in template functions:\n```cpp\ntemplate<typename T>\nvoid wrapper(T&& arg) {\n    foo(forward<T>(arg));  // Preserves lvalue/rvalue\n}\n```\n\n## Exception Safety Guarantees\n\n| Guarantee | Description |\n|-----------|-------------|\n| No-throw | Never throws exceptions |\n| Strong | Rollback on failure (transactional) |\n| Basic | No resource leaks, valid state |\n| No guarantee | Anything can happen |\n\n## RAII Summary\n\nResource Acquisition Is Initialization: tie resource lifetime to object lifetime. The foundation of C++ resource management:\n```cpp\nclass File {\n    FILE* f;\npublic:\n    File(const char* name) : f(fopen(name, \"r\")) { }\n    ~File() { if (f) fclose(f); }\n    // No copy, only move or unique ownership\n};\n```\n\n## Best Practices\n\n1. Use bit operations for flags, permissions, and performance-critical code\n2. Prefer `std::bitset` for fixed-size bit arrays\n3. Understand padding/alignment for memory-efficient structures\n4. Use `std::move` sparingly — trust the optimizer\n5. Always provide strong or basic exception guarantee',
    code_example: '#include <iostream>\n#include <bitset>\nusing namespace std;\n\nint main() {\n    unsigned int a = 0b101101;  // 45\n    unsigned int b = 0b011001;  // 25\n\n    cout << "a =     \" << bitset<8>(a) << \" (\" << a << \")\" << endl;\n    cout << "b =     \" << bitset<8>(b) << \" (\" << b << \")\" << endl;\n    cout << "a & b = \" << bitset<8>(a & b) << \" (\" << (a & b) << \")\" << endl;\n    cout << "a | b = \" << bitset<8>(a | b) << \" (\" << (a | b) << \")\" << endl;\n    cout << "a ^ b = \" << bitset<8>(a ^ b) << \" (\" << (a ^ b) << \")\" << endl;\n    cout << \"~a =    \" << bitset<8>(~a) << \" (\" << (~a) << \")\" << endl;\n    cout << \"a << 1 = \" << bitset<8>(a << 1) << \" (\" << (a << 1) << \")\" << endl;\n    cout << \"a >> 1 = \" << bitset<8>(a >> 1) << \" (\" << (a >> 1) << \")\" << endl;\n\n    int x = 16;\n    cout << x << \" is \" << (isPowerOfTwo(x) ? \"\" : \"not \") << \"power of 2\" << endl;\n    cout << \"Bits in 45: \" << __builtin_popcount(45) << endl;\n\n    return 0;\n}'
  }
];
export default advancedLessons;
