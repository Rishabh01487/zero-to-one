const treeLessons = [
  {
    id: 'lesson-46',
    title: 'Binary Trees & Traversals',
    category: 'trees',
    order_index: 46,
    content: '## What is a Tree?\n\nA tree is a hierarchical data structure consisting of nodes connected by edges. It has a root node and each node can have child nodes. Unlike linked lists (linear), trees are non-linear.\n\n## Binary Tree\n\nA binary tree is a tree where each node has at most TWO children (left and right).\n\n```cpp\nstruct TreeNode {\n    int data;\n    TreeNode* left;\n    TreeNode* right;\n\n    TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}\n};\n```\n\n## Types of Binary Trees\n\n| Type | Property |\n|------|----------|\n| Full | Every node has 0 or 2 children |\n| Complete | All levels filled except possibly last, which is left-filled |\n| Perfect | All internal nodes have 2 children, all leaves at same level |\n| Balanced | Height difference between left/right subtrees ≤ 1 for all nodes |\n| Degenerate | Each node has 1 child (effectively a linked list) |\n\n## Tree Traversals\n\n### Depth-First Search (DFS)\n\n**Inorder** (Left → Root → Right):\n```cpp\nvoid inorder(TreeNode* root) {\n    if (root == nullptr) return;\n    inorder(root->left);\n    cout << root->data << \" \";\n    inorder(root->right);\n}\n// Result: 4 2 5 1 6 3 7\n```\n\n**Preorder** (Root → Left → Right):\n```cpp\nvoid preorder(TreeNode* root) {\n    if (root == nullptr) return;\n    cout << root->data << \" \";\n    preorder(root->left);\n    preorder(root->right);\n}\n// Result: 1 2 4 5 3 6 7\n```\n\n**Postorder** (Left → Right → Root):\n```cpp\nvoid postorder(TreeNode* root) {\n    if (root == nullptr) return;\n    postorder(root->left);\n    postorder(root->right);\n    cout << root->data << \" \";\n}\n// Result: 4 5 2 6 7 3 1\n```\n\n### Breadth-First Search (Level Order)\n\n```cpp\nvoid levelOrder(TreeNode* root) {\n    if (root == nullptr) return;\n    queue<TreeNode*> q;\n    q.push(root);\n\n    while (!q.empty()) {\n        TreeNode* current = q.front(); q.pop();\n        cout << current->data << \" \";\n        if (current->left) q.push(current->left);\n        if (current->right) q.push(current->right);\n    }\n}\n// Result: 1 2 3 4 5 6 7\n```\n\n## Visualization\n\n```\n        1\n       / \\\n      2   3\n     / \\ / \\\n    4  5 6  7\n\nInorder:    4 2 5 1 6 3 7\nPreorder:   1 2 4 5 3 6 7\nPostorder:  4 5 2 6 7 3 1\nLevelOrder: 1 2 3 4 5 6 7\n```\n\n## Iterative Traversals\n\nInorder using explicit stack:\n```cpp\nvoid inorderIterative(TreeNode* root) {\n    stack<TreeNode*> st;\n    TreeNode* curr = root;\n\n    while (curr || !st.empty()) {\n        while (curr) { st.push(curr); curr = curr->left; }\n        curr = st.top(); st.pop();\n        cout << curr->data << \" \";\n        curr = curr->right;\n    }\n}\n```\n\n## Common Tree Problems\n\n1. Height of tree (max depth)\n2. Check if tree is balanced\n3. Diameter of tree (longest path between any two nodes)\n4. Lowest common ancestor (LCA)\n5. Mirror/invert tree\n\n## Best Practices\n1. Always check for nullptr (base case)\n2. Recursion is natural for trees\n3. For iterative, use stack (DFS) or queue (BFS)\n4. Draw the tree to trace your algorithm',
    code_example: '#include <iostream>\n#include <queue>\n#include <stack>\nusing namespace std;\n\nstruct TreeNode {\n    int data;\n    TreeNode *left, *right;\n    TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}\n};\n\nvoid inorder(TreeNode* r) { if (!r) return; inorder(r->left); cout << r->data << \" \"; inorder(r->right); }\nvoid preorder(TreeNode* r) { if (!r) return; cout << r->data << \" \"; preorder(r->left); preorder(r->right); }\nvoid postorder(TreeNode* r) { if (!r) return; postorder(r->left); postorder(r->right); cout << r->data << \" \"; }\n\nvoid levelOrder(TreeNode* root) {\n    if (!root) return;\n    queue<TreeNode*> q; q.push(root);\n    while (!q.empty()) {\n        TreeNode* cur = q.front(); q.pop();\n        cout << cur->data << \" \";\n        if (cur->left) q.push(cur->left);\n        if (cur->right) q.push(cur->right);\n    }\n}\n\nint treeHeight(TreeNode* r) {\n    if (!r) return 0;\n    return 1 + max(treeHeight(r->left), treeHeight(r->right));\n}\n\nint main() {\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n    root->right->left = new TreeNode(6);\n    root->right->right = new TreeNode(7);\n\n    cout << \"Inorder:    \"; inorder(root); cout << endl;\n    cout << \"Preorder:   \"; preorder(root); cout << endl;\n    cout << \"Postorder:  \"; postorder(root); cout << endl;\n    cout << \"LevelOrder: \"; levelOrder(root); cout << endl;\n    cout << \"Height: \" << treeHeight(root) << endl;\n\n    return 0;\n}'
  },
  {
    id: 'lesson-47',
    title: 'Binary Search Trees',
    category: 'trees',
    order_index: 47,
    content: '## What is a BST?\n\nA Binary Search Tree (BST) is a binary tree where every node follows the **BST property**:\n- Left subtree contains ONLY nodes with values **less than** the node\n- Right subtree contains ONLY nodes with values **greater than** the node\n- No duplicate nodes (typically)\n\n```cpp\nstruct TreeNode {\n    int val;\n    TreeNode* left;\n    TreeNode* right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n```\n\n## Why BST?\n\n| Operation | Unsorted Array | Sorted Array | BST |\n|-----------|---------------|-------------|-----|\n| Search | O(n) | O(log n)* | O(log n) |\n| Insert | O(1) | O(n) | O(log n) |\n| Delete | O(n) | O(n) | O(log n) |\n\n*Binary search on sorted array requires O(n) insert/delete.\n\n## BST Property Illustrated\n\n```\n        50\n       /  \\\n     30    70\n    /  \\   / \\\n   20  40 60 80\n\nAll left subtree of 50: {20, 30, 40} < 50\nAll right subtree of 50: {60, 70, 80} > 50\n```\n\nInorder traversal of a BST visits nodes in **ascending order**!\n\n## Checking if a Tree is BST\n\n```cpp\nbool isBST(TreeNode* root, long long minVal, long long maxVal) {\n    if (root == nullptr) return true;\n    if (root->val <= minVal || root->val >= maxVal) return false;\n    return isBST(root->left, minVal, root->val) &&\n           isBST(root->right, root->val, maxVal);\n}\n\nbool isValidBST(TreeNode* root) {\n    return isBST(root, LLONG_MIN, LLONG_MAX);\n}\n```\n\n## Inorder Successor\n\nThe next node in BST order:\n1. If node has right subtree: min of right subtree\n2. Else: find the ancestor where node is in left subtree\n\n```cpp\nTreeNode* inorderSuccessor(TreeNode* root, TreeNode* target) {\n    TreeNode* successor = nullptr;\n    while (root) {\n        if (target->val < root->val) {\n            successor = root;\n            root = root->left;\n        } else {\n            root = root->right;\n        }\n    }\n    return successor;\n}\n```\n\n## Applications\n1. Database indexing\n2. Autocomplete / dictionary\n3. Priority scheduling\n4. File system organization\n\n## Complexity\n\n| Case | Search | Insert | Delete |\n|------|--------|--------|--------|\n| Average | O(log n) | O(log n) | O(log n) |\n| Worst (skewed) | O(n) | O(n) | O(n) |\n\nWorst case occurs when inserting sorted data (degenerates to linked list). Use self-balancing trees (AVL, Red-Black) to avoid this.',
    code_example: '#include <iostream>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nTreeNode* insert(TreeNode* root, int val) {\n    if (!root) return new TreeNode(val);\n    if (val < root->val) root->left = insert(root->left, val);\n    else if (val > root->val) root->right = insert(root->right, val);\n    return root;\n}\n\nbool search(TreeNode* root, int val) {\n    if (!root) return false;\n    if (root->val == val) return true;\n    if (val < root->val) return search(root->left, val);\n    return search(root->right, val);\n}\n\nvoid inorder(TreeNode* r) {\n    if (!r) return;\n    inorder(r->left);\n    cout << r->val << \" \";\n    inorder(r->right);\n}\n\nint main() {\n    TreeNode* root = nullptr;\n    int vals[] = {50, 30, 70, 20, 40, 60, 80};\n    for (int v : vals) root = insert(root, v);\n\n    cout << "BST inorder: \"; inorder(root); cout << endl;\n    cout << \"Search 40: \" << (search(root, 40) ? \"Found\" : \"Not found\") << endl;\n    cout << \"Search 55: \" << (search(root, 55) ? \"Found\" : \"Not found\") << endl;\n\n    return 0;\n}'
  },
  {
    id: 'lesson-48',
    title: 'BST Operations (Insert, Delete, Search)',
    category: 'trees',
    order_index: 48,
    content: '## BST Operations in Depth\n\nMastering BST operations is essential for understanding tree-based data structures.\n\n## 1. Search\n\n```cpp\nTreeNode* search(TreeNode* root, int key) {\n    if (root == nullptr || root->val == key)\n        return root;\n\n    if (key < root->val)\n        return search(root->left, key);\n    return search(root->right, key);\n}\n```\n\n**Iterative version**:\n```cpp\nTreeNode* searchIterative(TreeNode* root, int key) {\n    while (root != nullptr && root->val != key) {\n        if (key < root->val) root = root->left;\n        else root = root->right;\n    }\n    return root;\n}\n```\n\n## 2. Insert\n\n```cpp\nTreeNode* insert(TreeNode* root, int key) {\n    if (root == nullptr)\n        return new TreeNode(key);\n\n    if (key < root->val)\n        root->left = insert(root->left, key);\n    else if (key > root->val)\n        root->right = insert(root->right, key);\n\n    // key == root->val: no duplicates (do nothing)\n    return root;\n}\n```\n\n## 3. Delete\n\nThree cases:\n1. **Leaf node**: Simply remove\n2. **One child**: Replace with the child\n3. **Two children**: Find inorder successor (smallest in right subtree), copy its value, delete it\n\n```cpp\nTreeNode* findMin(TreeNode* root) {\n    while (root->left != nullptr) root = root->left;\n    return root;\n}\n\nTreeNode* deleteNode(TreeNode* root, int key) {\n    if (root == nullptr) return nullptr;\n\n    if (key < root->val)\n        root->left = deleteNode(root->left, key);\n    else if (key > root->val)\n        root->right = deleteNode(root->right, key);\n    else {\n        // Case 1: Leaf\n        if (root->left == nullptr && root->right == nullptr) {\n            delete root;\n            return nullptr;\n        }\n        // Case 2: One child\n        if (root->left == nullptr) {\n            TreeNode* temp = root->right;\n            delete root;\n            return temp;\n        }\n        if (root->right == nullptr) {\n            TreeNode* temp = root->left;\n            delete root;\n            return temp;\n        }\n        // Case 3: Two children\n        TreeNode* successor = findMin(root->right);\n        root->val = successor->val;\n        root->right = deleteNode(root->right, successor->val);\n    }\n    return root;\n}\n```\n\n## Visualizing Deletion\n\n```\nDelete 50 (two children):\n        50                        60\n       /  \\                     /  \\\n     30    70        →        30    70\n    /  \\   / \\              /  \\     \\\n   20  40 60 80           20  40     80\n\nDelete 40 (leaf):\n        50                        50\n       /  \\                     /  \\\n     30    70        →        30    70\n    /  \\   / \\              /      / \\\n   20  40 60 80           20     60 80\n\nDelete 30 (one child):\n        50                        50\n       /  \\                     /  \\\n     30    70        →        20    70\n    /       / \\                     / \\\n   20      60 80                  60 80\n```\n\n## Complexity Recap\n\n| Operation | Average | Worst (Skewed) |\n|-----------|---------|---------------|\n| Search | O(log n) | O(n) |\n| Insert | O(log n) | O(n) |\n| Delete | O(log n) | O(n) |\n\n## Best Practices\n1. Recursive implementations are cleaner but use O(h) stack space\n2. Iterative versions use O(1) space\n3. For sorted data input, BST degenerates — use AVL or Red-Black tree\n4. Avoid recursive delete on deep trees (stack overflow)',
    code_example: '#include <iostream>\nusing namespace std;\n\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nTreeNode* insert(TreeNode* r, int v) {\n    if (!r) return new TreeNode(v);\n    if (v < r->val) r->left = insert(r->left, v);\n    else if (v > r->val) r->right = insert(r->right, v);\n    return r;\n}\n\nTreeNode* findMin(TreeNode* r) {\n    while (r && r->left) r = r->left;\n    return r;\n}\n\nTreeNode* deleteNode(TreeNode* r, int key) {\n    if (!r) return nullptr;\n    if (key < r->val) r->left = deleteNode(r->left, key);\n    else if (key > r->val) r->right = deleteNode(r->right, key);\n    else {\n        if (!r->left && !r->right) { delete r; return nullptr; }\n        if (!r->left) { TreeNode* t = r->right; delete r; return t; }\n        if (!r->right) { TreeNode* t = r->left; delete r; return t; }\n        TreeNode* s = findMin(r->right);\n        r->val = s->val;\n        r->right = deleteNode(r->right, s->val);\n    }\n    return r;\n}\n\nvoid inorder(TreeNode* r) {\n    if (!r) return; inorder(r->left); cout << r->val << \" \"; inorder(r->right);\n}\n\nint main() {\n    TreeNode* root = nullptr;\n    for (int v : {50, 30, 70, 20, 40, 60, 80}) root = insert(root, v);\n    cout << \"Original: \"; inorder(root); cout << endl;\n    root = deleteNode(root, 50);\n    cout << \"After del 50: \"; inorder(root); cout << endl;\n    root = deleteNode(root, 20);\n    cout << \"After del 20: \"; inorder(root); cout << endl;\n    root = deleteNode(root, 30);\n    cout << \"After del 30: \"; inorder(root); cout << endl;\n    return 0;\n}'
  },
  {
    id: 'lesson-49',
    title: 'AVL Trees & Balancing',
    category: 'trees',
    order_index: 49,
    content: '## Why Balanced Trees?\n\nA BST can degenerate into a linked list when inserting sorted data, making all operations O(n). Balanced trees maintain O(log n) height by automatically rebalancing.\n\n## AVL Tree (Adelson-Velsky and Landis)\n\nAn AVL tree is a self-balancing BST where the height difference between left and right subtrees (balance factor) is at most 1 for every node.\n\n**Balance Factor** = `height(left) - height(right)`\n\nAllowed values: -1, 0, +1\n\n## Node Structure\n\n```cpp\nstruct AVLNode {\n    int key;\n    AVLNode* left;\n    AVLNode* right;\n    int height;  // Height of subtree rooted at this node\n\n    AVLNode(int k) : key(k), left(nullptr), right(nullptr), height(1) {}\n};\n```\n\n## Rotations\n\nFour cases when balance factor is violated:\n\n### Left-Left (LL) — Right Rotate\n\n```\n    z (bf=2)          y\n   /                  / \\\n  y (bf=1)    →      x   z\n /\nx\n```\n\n```cpp\nAVLNode* rightRotate(AVLNode* z) {\n    AVLNode* y = z->left;\n    AVLNode* T3 = y->right;\n\n    y->right = z;\n    z->left = T3;\n\n    z->height = 1 + max(height(z->left), height(z->right));\n    y->height = 1 + max(height(y->left), height(y->right));\n\n    return y;  // New root\n}\n```\n\n### Right-Right (RR) — Left Rotate\n\n```cpp\nAVLNode* leftRotate(AVLNode* z) {\n    AVLNode* y = z->right;\n    AVLNode* T2 = y->left;\n\n    y->left = z;\n    z->right = T2;\n\n    z->height = 1 + max(height(z->left), height(z->right));\n    y->height = 1 + max(height(y->left), height(y->right));\n\n    return y;\n}\n```\n\n### Left-Right (LR) — Left rotate then Right rotate\n\n### Right-Left (RL) — Right rotate then Left rotate\n\n## Complete Insert\n\n```cpp\nAVLNode* insert(AVLNode* node, int key) {\n    // 1. Standard BST insert\n    if (!node) return new AVLNode(key);\n    if (key < node->key) node->left = insert(node->left, key);\n    else if (key > node->key) node->right = insert(node->right, key);\n    else return node;  // No duplicates\n\n    // 2. Update height\n    node->height = 1 + max(height(node->left), height(node->right));\n\n    // 3. Get balance factor\n    int balance = getBalance(node);\n\n    // 4. Rotate if unbalanced\n    // LL\n    if (balance > 1 && key < node->left->key) return rightRotate(node);\n    // RR\n    if (balance < -1 && key > node->right->key) return leftRotate(node);\n    // LR\n    if (balance > 1 && key > node->left->key) {\n        node->left = leftRotate(node->left);\n        return rightRotate(node);\n    }\n    // RL\n    if (balance < -1 && key < node->right->key) {\n        node->right = rightRotate(node->right);\n        return leftRotate(node);\n    }\n\n    return node;\n}\n```\n\n## Performance\n\n| Operation | AVL | BST (worst) |\n|-----------|-----|-------------|\n| Search | O(log n) | O(n) |\n| Insert | O(log n) | O(n) |\n| Delete | O(log n) | O(n) |\n\n## Comparison: AVL vs Red-Black\n\n| Aspect | AVL | Red-Black |\n|--------|-----|-----------|\n| Balancing | Stricter (bf ≤ 1) | Looser (no double red) |\n| Lookup | Faster (more balanced) | Slightly slower |\n| Insert/Delete | More rotations | Fewer rotations |\n| Use case | Lookup-heavy | Write-heavy |\n\nC++ `std::set` and `std::map` use Red-Black trees.',
    code_example: '#include <iostream>\n#include <algorithm>\nusing namespace std;\n\nstruct AVLNode {\n    int key;\n    AVLNode *left, *right;\n    int height;\n    AVLNode(int k) : key(k), left(nullptr), right(nullptr), height(1) {}\n};\n\nint height(AVLNode* n) { return n ? n->height : 0; }\nint balanceFactor(AVLNode* n) { return n ? height(n->left) - height(n->right) : 0; }\n\nAVLNode* rightRotate(AVLNode* y) {\n    AVLNode* x = y->left;\n    AVLNode* T2 = x->right;\n    x->right = y; y->left = T2;\n    y->height = 1 + max(height(y->left), height(y->right));\n    x->height = 1 + max(height(x->left), height(x->right));\n    return x;\n}\n\nAVLNode* leftRotate(AVLNode* x) {\n    AVLNode* y = x->right;\n    AVLNode* T2 = y->left;\n    y->left = x; x->right = T2;\n    x->height = 1 + max(height(x->left), height(x->right));\n    y->height = 1 + max(height(y->left), height(y->right));\n    return y;\n}\n\nAVLNode* insert(AVLNode* node, int key) {\n    if (!node) return new AVLNode(key);\n    if (key < node->key) node->left = insert(node->left, key);\n    else if (key > node->key) node->right = insert(node->right, key);\n    else return node;\n\n    node->height = 1 + max(height(node->left), height(node->right));\n    int bf = balanceFactor(node);\n\n    if (bf > 1 && key < node->left->key) return rightRotate(node);\n    if (bf < -1 && key > node->right->key) return leftRotate(node);\n    if (bf > 1 && key > node->left->key) { node->left = leftRotate(node->left); return rightRotate(node); }\n    if (bf < -1 && key < node->right->key) { node->right = rightRotate(node->right); return leftRotate(node); }\n    return node;\n}\n\nvoid preorder(AVLNode* r) {\n    if (!r) return;\n    cout << r->key << \"(\" << balanceFactor(r) << \") \";\n    preorder(r->left);\n    preorder(r->right);\n}\n\nint main() {\n    AVLNode* root = nullptr;\n    for (int v : {10, 20, 30, 40, 50, 25}) root = insert(root, v);\n    cout << \"Preorder with balance factors: \";\n    preorder(root); cout << endl;\n    cout << \"Tree height: \" << height(root) << endl;\n    return 0;\n}'
  },
  {
    id: 'lesson-50',
    title: 'Heap & Priority Queue',
    category: 'trees',
    order_index: 50,
    content: '## Binary Heap\n\nA binary heap is a complete binary tree that satisfies the heap property:\n- **Max-heap**: Parent ≥ children\n- **Min-heap**: Parent ≤ children\n\nHeaps are commonly implemented using arrays for space efficiency.\n\n## Array Representation\n\n```\nIndex:  0  1  2  3  4  5  6\nValue: 90 80 70 50 40 30 20\n\n       90\n     /    \\\n    80     70\n   /  \\   /  \\\n 50   40 30   20\n```\n\nFor node at index `i`:\n- Parent: `(i - 1) / 2`\n- Left child: `2*i + 1`\n- Right child: `2*i + 2`\n\n## Core Operations\n\n### Push (Insert)\n\n1. Add to end of array\n2. Bubble up: while parent < node (for max-heap), swap\n\n```cpp\nvoid push(vector<int>& heap, int val) {\n    heap.push_back(val);\n    int i = heap.size() - 1;\n\n    while (i > 0 && heap[(i - 1) / 2] < heap[i]) {\n        swap(heap[i], heap[(i - 1) / 2]);\n        i = (i - 1) / 2;\n    }\n}\n```\n\nO(log n) time.\n\n### Pop (Remove Max)\n\n1. Replace root with last element\n2. Remove last\n3. Sift down: while child > parent, swap with larger child\n\n```cpp\nvoid heapify(vector<int>& heap, int n, int i) {\n    int largest = i;\n    int left = 2 * i + 1;\n    int right = 2 * i + 2;\n\n    if (left < n && heap[left] > heap[largest]) largest = left;\n    if (right < n && heap[right] > heap[largest]) largest = right;\n\n    if (largest != i) {\n        swap(heap[i], heap[largest]);\n        heapify(heap, n, largest);\n    }\n}\n\nvoid pop(vector<int>& heap) {\n    heap[0] = heap.back();\n    heap.pop_back();\n    heapify(heap, heap.size(), 0);\n}\n```\n\nO(log n) time.\n\n### Build Heap\n\n```cpp\nvoid buildHeap(vector<int>& heap) {\n    for (int i = heap.size() / 2 - 1; i >= 0; i--)\n        heapify(heap, heap.size(), i);\n}\n```\n\nO(n) time (not O(n log n)!).\n\n## std::priority_queue\n\nC++ provides a ready-made heap via `std::priority_queue`:\n\n```cpp\n#include <queue>\n\n// Max heap (default)\npriority_queue<int> pq;\npq.push(10);\npq.push(5);\npq.push(20);\ncout << pq.top();  // 20\npq.pop();           // Removes 20\n\n// Min heap\npriority_queue<int, vector<int>, greater<int>> minPQ;\n\n// Custom\npriority_queue< pair<int,int>, vector<pair<int,int>>, greater<...> >;  // Dijkstra\n```\n\n## Applications\n\n1. **Priority scheduling**: OS task scheduling\n2. **Dijkstra\'s algorithm**: Shortest path\n3. **Huffman coding**: Data compression\n4. **Median maintenance**: Two heaps (min + max)\n5. **K-th largest element**: Min-heap of size k\n6. **Merging K sorted arrays**: Min-heap of iterators\n\n## Heap Sort\n\n1. Build max-heap\n2. Repeatedly extract max (swap root with last, reduce size, heapify)\n\nO(n log n) time, O(1) space.\n\n## Best Practices\n\n1. Use `std::priority_queue` unless you need raw array access\n2. For custom types, provide comparator or operator<\n3. Build heap in O(n) instead of n × O(log n) individual inserts\n4. Min-heap for smallest elements, max-heap for largest',
    code_example: '#include <iostream>\n#include <queue>\n#include <vector>\nusing namespace std;\n\nint main() {\n    priority_queue<int> maxHeap;\n    priority_queue<int, vector<int>, greater<int>> minHeap;\n\n    int nums[] = {3, 1, 4, 1, 5, 9, 2, 6};\n    for (int n : nums) {\n        maxHeap.push(n);\n        minHeap.push(n);\n    }\n\n    cout << \"Max heap: \";\n    while (!maxHeap.empty()) {\n        cout << maxHeap.top() << \" \";\n        maxHeap.pop();\n    }\n    cout << endl;\n\n    cout << \"Min heap: \";\n    while (!minHeap.empty()) {\n        cout << minHeap.top() << \" \";\n        minHeap.pop();\n    }\n    cout << endl;\n\n    // Custom: process tasks by priority\n    priority_queue<pair<int, string>> tasks;\n    tasks.push({3, \"Low\"});\n    tasks.push({10, \"High\"});\n    tasks.push({7, \"Medium\"});\n    cout << \"Next task: \" << tasks.top().second << endl;\n\n    return 0;\n}'
  }
];
export default treeLessons;
