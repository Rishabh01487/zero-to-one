const sortingLessons = [
  {
    id: 'lesson-37',
    title: 'Selection & Insertion Sort',
    category: 'sorting',
    order_index: 37,
    content: '## Why Learn Basic Sorts?\n\nSimple sorting algorithms teach fundamental concepts like loops, swapping, and invariants. While not practical for large datasets, they are building blocks for understanding more complex algorithms.\n\n## Selection Sort\n\nRepeatedly select the minimum element and swap it into position.\n\n```cpp\nvoid selectionSort(int arr[], int n) {\n    for (int i = 0; i < n - 1; i++) {\n        int minIdx = i;\n        for (int j = i + 1; j < n; j++) {\n            if (arr[j] < arr[minIdx]) minIdx = j;\n        }\n        swap(arr[i], arr[minIdx]);\n    }\n}\n```\n\n### How It Works\n\nPass by pass on `[64, 25, 12, 22, 11]`:\n```\nPass 1: [11, 25, 12, 22, 64]  (min=11, swap with 64)\nPass 2: [11, 12, 25, 22, 64]  (min=12, swap with 25)\nPass 3: [11, 12, 22, 25, 64]  (min=22, swap with 25)\nPass 4: [11, 12, 22, 25, 64]  (min=25, swap with 25)\n```\n\n**Complexity**: O(n²) time, O(1) space. Always O(n²) — no best-case improvement.\n\n## Insertion Sort\n\nBuild the sorted array one element at a time by inserting each element into its correct position.\n\n```cpp\nvoid insertionSort(int arr[], int n) {\n    for (int i = 1; i < n; i++) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= 0 && arr[j] > key) {\n            arr[j + 1] = arr[j];\n            j--;\n        }\n        arr[j + 1] = key;\n    }\n}\n```\n\n### How It Works\n\n```\n[64, 25, 12, 22, 11]\nPass 1: [25, 64, 12, 22, 11]  (insert 25)\nPass 2: [12, 25, 64, 22, 11]  (insert 12)\nPass 3: [12, 22, 25, 64, 11]  (insert 22)\nPass 4: [11, 12, 22, 25, 64]  (insert 11)\n```\n\n**Complexity**: O(n²) worst, O(n) best (already sorted!). O(1) space.\n\n## Comparison\n\n| Aspect | Selection Sort | Insertion Sort |\n|--------|---------------|---------------|\n| Best case | O(n²) | O(n) |\n| Swaps | O(n) | O(n²) |\n| Comparisons | O(n²) | O(n²) |\n| Stable? | No | Yes |\n| Adaptive? | No | Yes |\n\n## When to Use\n\n- **Selection Sort**: When memory writes are expensive (minimal swaps)\n- **Insertion Sort**: Small arrays, nearly sorted data, online algorithm (sort as data arrives)\n\n## Practice Tips\n1. Trace through each pass on paper\n2. Implement both from memory\n3. Test with edge cases (empty, single element, already sorted, reverse sorted)',
    code_example: '#include <iostream>\nusing namespace std;\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n    cout << endl;\n}\n\nvoid selectionSort(int arr[], int n) {\n    for (int i = 0; i < n - 1; i++) {\n        int minIdx = i;\n        for (int j = i + 1; j < n; j++)\n            if (arr[j] < arr[minIdx]) minIdx = j;\n        swap(arr[i], arr[minIdx]);\n        cout << \"Pass \" << i+1 << \": \"; printArray(arr, n);\n    }\n}\n\nvoid insertionSort(int arr[], int n) {\n    for (int i = 1; i < n; i++) {\n        int key = arr[i], j = i - 1;\n        while (j >= 0 && arr[j] > key) {\n            arr[j + 1] = arr[j]; j--;\n        }\n        arr[j + 1] = key;\n        cout << \"Pass \" << i << \": \"; printArray(arr, n);\n    }\n}\n\nint main() {\n    int arr1[] = {64, 25, 12, 22, 11};\n    int n = sizeof(arr1)/sizeof(arr1[0]);\n    cout << \"Selection Sort:\\n\";\n    selectionSort(arr1, n);\n\n    int arr2[] = {64, 25, 12, 22, 11};\n    cout << \"\\nInsertion Sort:\\n\";\n    insertionSort(arr2, n);\n\n    return 0;\n}'
  },
  {
    id: 'lesson-38',
    title: 'Merge Sort',
    category: 'sorting',
    order_index: 38,
    content: '## Divide and Conquer\n\nMerge Sort is a classic divide-and-conquer algorithm. It splits the array, sorts each half recursively, then merges the sorted halves.\n\n## Algorithm\n\n1. **Divide**: Split the array into two halves\n2. **Conquer**: Recursively sort each half\n3. **Combine**: Merge the two sorted halves\n\n```cpp\nvoid mergeSort(int arr[], int left, int right) {\n    if (left >= right) return;  // Base case: single element\n\n    int mid = left + (right - left) / 2;\n    mergeSort(arr, left, mid);\n    mergeSort(arr, mid + 1, right);\n    merge(arr, left, mid, right);\n}\n```\n\n## The Merge Step\n\nMerging two sorted arrays into one sorted array:\n\n```cpp\nvoid merge(int arr[], int left, int mid, int right) {\n    int n1 = mid - left + 1;\n    int n2 = right - mid;\n\n    int* L = new int[n1];\n    int* R = new int[n2];\n\n    // Copy data\n    for (int i = 0; i < n1; i++) L[i] = arr[left + i];\n    for (int j = 0; j < n2; j++) R[j] = arr[mid + 1 + j];\n\n    // Merge\n    int i = 0, j = 0, k = left;\n    while (i < n1 && j < n2) {\n        if (L[i] <= R[j]) arr[k++] = L[i++];\n        else arr[k++] = R[j++];\n    }\n\n    // Copy remaining\n    while (i < n1) arr[k++] = L[i++];\n    while (j < n2) arr[k++] = R[j++];\n\n    delete[] L;\n    delete[] R;\n}\n```\n\n## Visualization\n\n```\n[38, 27, 43, 3, 9, 82, 10]\n              /                    \\\n    [38, 27, 43, 3]          [9, 82, 10]\n     /          \\             /        \\\n [38, 27]    [43, 3]       [9, 82]   [10]\n  /    \\      /    \\        /    \\\n[38]  [27]  [43]  [3]    [9]   [82]\n  \\    /      \\    /        \\    /\n [27, 38]    [3, 43]       [9, 82]   [10]\n     \\          /             \\        /\n   [3, 27, 38, 43]          [9, 10, 82]\n              \\              /\n         [3, 9, 10, 27, 38, 43, 82]\n```\n\n## Complexity\n\n| Case | Time | Space |\n|------|------|-------|\n| Best | O(n log n) | O(n) |\n| Average | O(n log n) | O(n) |\n| Worst | O(n log n) | O(n) |\n\n## Properties\n\n- **Stable**: Yes (if merge preserves order of equal elements)\n- **In-place**: No (needs O(n) extra space)\n- **Predictable**: Always O(n log n), regardless of input\n\n## Optimizations\n\n1. Use insertion sort for small subarrays (< 15-30 elements)\n2. Avoid copying by alternating between two arrays\n3. Check if already sorted: if `arr[mid] <= arr[mid+1]`, skip merge\n\n## Practice\n\n1. Implement iterative (bottom-up) merge sort\n2. Count inversions using merge sort modification\n3. Implement merge sort for linked lists (O(1) extra space)',
    code_example: '#include <iostream>\nusing namespace std;\n\nvoid merge(int arr[], int left, int mid, int right) {\n    int n1 = mid - left + 1, n2 = right - mid;\n    int* L = new int[n1];\n    int* R = new int[n2];\n    for (int i = 0; i < n1; i++) L[i] = arr[left + i];\n    for (int j = 0; j < n2; j++) R[j] = arr[mid + 1 + j];\n    int i = 0, j = 0, k = left;\n    while (i < n1 && j < n2) arr[k++] = (L[i] <= R[j]) ? L[i++] : R[j++];\n    while (i < n1) arr[k++] = L[i++];\n    while (j < n2) arr[k++] = R[j++];\n    delete[] L;\n    delete[] R;\n}\n\nvoid mergeSort(int arr[], int left, int right) {\n    if (left >= right) return;\n    int mid = left + (right - left) / 2;\n    mergeSort(arr, left, mid);\n    mergeSort(arr, mid + 1, right);\n    merge(arr, left, mid, right);\n}\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n    cout << endl;\n}\n\nint main() {\n    int arr[] = {38, 27, 43, 3, 9, 82, 10};\n    int n = sizeof(arr)/sizeof(arr[0]);\n    cout << \"Original: \"; printArray(arr, n);\n    mergeSort(arr, 0, n - 1);\n    cout << \"Sorted:   \"; printArray(arr, n);\n    return 0;\n}'
  },
  {
    id: 'lesson-39',
    title: 'Quick Sort',
    category: 'sorting',
    order_index: 39,
    content: '## Quick Sort\n\nQuick Sort is a divide-and-conquer algorithm that selects a "pivot" and partitions the array around it. Despite O(n²) worst case, it\'s often faster than merge sort in practice due to good cache locality.\n\n## Algorithm\n\n1. **Choose a pivot** (last element, first, random, median-of-three)\n2. **Partition**: Reorder so elements < pivot come before it and elements > pivot come after\n3. **Recurse**: Apply quick sort to the left and right partitions\n\n```cpp\nvoid quickSort(int arr[], int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);  // Partition index\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}\n```\n\n## Lomuto Partition Scheme\n\nSimplest partition: use last element as pivot.\n\n```cpp\nint partition(int arr[], int low, int high) {\n    int pivot = arr[high];\n    int i = low - 1;  // Index of smaller element\n\n    for (int j = low; j < high; j++) {\n        if (arr[j] < pivot) {\n            i++;\n            swap(arr[i], arr[j]);\n        }\n    }\n    swap(arr[i + 1], arr[high]);\n    return i + 1;\n}\n```\n\n### Step-by-step\n\n```\nPivot = last element (4)\n[3, 7, 8, 5, 2, 1, 9, 5, 4]\n                         ^pivot\n\nAfter partition:\n[3, 2, 1, 4, 7, 8, 5, 9, 5]\n          ^pivot in correct position\n          left < 4    |    right >= 4\n```\n\n## Hoare Partition (Faster)\n\nUses two pointers from both ends. Typically 3x faster than Lomuto but trickier.\n\n```cpp\nint partition(int arr[], int low, int high) {\n    int pivot = arr[low + (high - low) / 2];\n    int i = low - 1, j = high + 1;\n    while (true) {\n        do i++; while (arr[i] < pivot);\n        do j--; while (arr[j] > pivot);\n        if (i >= j) return j;\n        swap(arr[i], arr[j]);\n    }\n}\n```\n\n## Complexity\n\n| Case | Time | Space |\n|------|------|-------|\n| Best | O(n log n) | O(log n) |\n| Average | O(n log n) | O(log n) |\n| Worst | O(n²) | O(n) |\n\nWorst case occurs when pivot is always the smallest or largest element (already sorted array with naive pivot choice).\n\n## Optimizations\n\n1. **Random pivot**: Pick random index to avoid worst-case\n2. **Median-of-three**: Pivot = median of first, middle, last\n3. **Introsort**: Switch to heap sort if recursion depth exceeds log n\n4. **Three-way partition**: Handle duplicate elements efficiently (Dutch National Flag)\n\n## Quick Select\n\nFind the k-th smallest element without fully sorting:\n```cpp\nint quickSelect(int arr[], int low, int high, int k) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        if (pi == k) return arr[pi];\n        if (k < pi) return quickSelect(arr, low, pi - 1, k);\n        else return quickSelect(arr, pi + 1, high, k);\n    }\n    return arr[low];\n}\n```\nO(n) average time.\n\n## Best Practices\n1. Always use random or median-of-three pivot\n2. Switch to insertion sort for small subarrays\n3. Quick sort is NOT stable\n4. C++ `std::sort` uses Introsort (quick + heap + insertion)',
    code_example: '#include <iostream>\nusing namespace std;\n\nint partition(int arr[], int low, int high) {\n    int pivot = arr[high];\n    int i = low - 1;\n    for (int j = low; j < high; j++) {\n        if (arr[j] < pivot) swap(arr[++i], arr[j]);\n    }\n    swap(arr[i + 1], arr[high]);\n    return i + 1;\n}\n\nvoid quickSort(int arr[], int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n    cout << endl;\n}\n\nint main() {\n    int arr[] = {10, 7, 8, 9, 1, 5};\n    int n = sizeof(arr)/sizeof(arr[0]);\n    cout << \"Original: \"; printArray(arr, n);\n    quickSort(arr, 0, n - 1);\n    cout << \"Sorted:   \"; printArray(arr, n);\n    return 0;\n}'
  },
  {
    id: 'lesson-40',
    title: 'Heap Sort',
    category: 'sorting',
    order_index: 40,
    content: '## Heap Sort\n\nHeap Sort uses a binary heap data structure to sort elements. It\'s an in-place, O(n log n) algorithm with no worst-case performance issues (unlike quick sort).\n\n## Prerequisites: Binary Heap\n\nA binary heap is a complete binary tree where:\n- **Max-heap**: Parent >= children\n- **Min-heap**: Parent <= children\n\nArray representation:\n```\nIndex:  0  1  2  3  4  5  6\nValue: 90 80 70 50 40 30 20\n\nTree:\n       90\n     /    \\\n    80     70\n   /  \\   /  \\\n 50   40 30   20\n```\n\nFor node at index `i`:\n- Left child: `2*i + 1`\n- Right child: `2*i + 2`\n- Parent: `(i - 1) / 2`\n\n## Heapify\n\nMaintains the heap property for a subtree rooted at index `i`:\n\n```cpp\nvoid heapify(int arr[], int n, int i) {\n    int largest = i;\n    int left = 2 * i + 1;\n    int right = 2 * i + 2;\n\n    if (left < n && arr[left] > arr[largest]) largest = left;\n    if (right < n && arr[right] > arr[largest]) largest = right;\n\n    if (largest != i) {\n        swap(arr[i], arr[largest]);\n        heapify(arr, n, largest);  // Recursively fix affected subtree\n    }\n}\n```\n\nO(log n) per call.\n\n## Algorithm\n\n1. **Build max-heap**: Heapify all non-leaf nodes from bottom-up\n2. **Extract max**: Swap root (max) with last element, reduce heap size, heapify root\n\n```cpp\nvoid heapSort(int arr[], int n) {\n    // Build max heap\n    for (int i = n / 2 - 1; i >= 0; i--)\n        heapify(arr, n, i);\n\n    // Extract elements one by one\n    for (int i = n - 1; i > 0; i--) {\n        swap(arr[0], arr[i]);      // Move max to end\n        heapify(arr, i, 0);        // Heapify reduced heap\n    }\n}\n```\n\n### Visualization\n\n```\nInitial: [12, 11, 13, 5, 6, 7]\n\nBuild max-heap:\n       13\n     /    \\\n   11     12\n  /  \\   /\n 5   6  7\n\nExtract max repeatedly:\n[13, 11, 12, 5, 6, 7]  // Heap\n[12, 11, 7, 5, 6, 13]  // Moved 13, heapify\n[11, 6, 7, 5, 12, 13]\n[7, 6, 5, 11, 12, 13]\n[6, 5, 7, 11, 12, 13]\n[5, 6, 7, 11, 12, 13]  // Sorted!\n```\n\n## Complexity\n\n| Operation | Time |\n|-----------|------|\n| Build heap | O(n) |\n| Each extract | O(log n) |\n| Total | O(n log n) |\n| Space | O(1) in-place |\n\n## Properties\n\n- **Not stable**: Equal elements may be reordered\n- **In-place**: Yes (O(1) extra space)\n- **Consistent**: Always O(n log n), no worst-case issues\n\n## When to Use\n\n- When you need guaranteed O(n log n) performance\n- When memory is limited (in-place)\n- Priority queue implementations\n\n## Comparison\n\n| Algorithm | Best | Average | Worst | Space | Stable |\n|-----------|------|---------|-------|-------|--------|\n| Heap Sort | O(n log n) | O(n log n) | O(n log n) | O(1) | No |\n| Quick Sort | O(n log n) | O(n log n) | O(n²) | O(log n) | No |\n| Merge Sort | O(n log n) | O(n log n) | O(n log n) | O(n) | Yes |',
    code_example: '#include <iostream>\nusing namespace std;\n\nvoid heapify(int arr[], int n, int i) {\n    int largest = i, left = 2*i + 1, right = 2*i + 2;\n    if (left < n && arr[left] > arr[largest]) largest = left;\n    if (right < n && arr[right] > arr[largest]) largest = right;\n    if (largest != i) {\n        swap(arr[i], arr[largest]);\n        heapify(arr, n, largest);\n    }\n}\n\nvoid heapSort(int arr[], int n) {\n    for (int i = n/2 - 1; i >= 0; i--) heapify(arr, n, i);\n    for (int i = n - 1; i > 0; i--) {\n        swap(arr[0], arr[i]);\n        heapify(arr, i, 0);\n    }\n}\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n    cout << endl;\n}\n\nint main() {\n    int arr[] = {12, 11, 13, 5, 6, 7};\n    int n = sizeof(arr)/sizeof(arr[0]);\n    cout << \"Original: \"; printArray(arr, n);\n    heapSort(arr, n);\n    cout << \"Sorted:   \"; printArray(arr, n);\n    return 0;\n}'
  },
  {
    id: 'lesson-41',
    title: 'Counting & Radix Sort',
    category: 'sorting',
    order_index: 41,
    content: '## Non-Comparison Sorts\n\nCounting Sort and Radix Sort are non-comparison-based sorting algorithms. They achieve O(n + k) time by exploiting the structure of keys, not by comparing elements.\n\n## Counting Sort\n\nCounts occurrences of each distinct element, then uses prefix sums to place elements in correct positions.\n\n**Works only for integers in a limited range `[0, k]`.**\n\n```cpp\nvoid countingSort(int arr[], int n, int k) {\n    // Count occurrences\n    vector<int> count(k + 1, 0);\n    for (int i = 0; i < n; i++) count[arr[i]]++;\n\n    // Prefix sum (positions)\n    for (int i = 1; i <= k; i++) count[i] += count[i - 1];\n\n    // Build output array (stable!)\n    vector<int> output(n);\n    for (int i = n - 1; i >= 0; i--) {\n        output[count[arr[i]] - 1] = arr[i];\n        count[arr[i]]--;\n    }\n\n    // Copy back\n    for (int i = 0; i < n; i++) arr[i] = output[i];\n}\n```\n\n### Visualization\n\n```\nInput:  [1, 4, 1, 2, 7, 5, 2]\nRange:  0-7\n\nCount:  [0, 2, 2, 0, 1, 1, 0, 1]\nPrefix: [0, 2, 4, 4, 5, 6, 6, 7]\nOutput: [1, 1, 2, 2, 4, 5, 7]\n```\n\n**Complexity**: O(n + k) time, O(n + k) space. **Stable**: Yes.\n\n## Radix Sort\n\nSorts numbers digit by digit, from least significant digit (LSD) to most significant. Uses counting sort as a subroutine for each digit.\n\n```cpp\nvoid radixSort(int arr[], int n) {\n    int maxVal = *max_element(arr, arr + n);\n    for (int exp = 1; maxVal / exp > 0; exp *= 10) {\n        countingSortByDigit(arr, n, exp);\n    }\n}\n\nvoid countingSortByDigit(int arr[], int n, int exp) {\n    vector<int> count(10, 0);  // 10 possible digits\n    vector<int> output(n);\n\n    for (int i = 0; i < n; i++) count[(arr[i] / exp) % 10]++;\n    for (int i = 1; i < 10; i++) count[i] += count[i - 1];\n    for (int i = n - 1; i >= 0; i--) {\n        output[count[(arr[i] / exp) % 10] - 1] = arr[i];\n        count[(arr[i] / exp) % 10]--;\n    }\n    for (int i = 0; i < n; i++) arr[i] = output[i];\n}\n```\n\n### Example: [170, 45, 75, 90, 802, 24, 2, 66]\n\n```\nOnes digit: [170, 90, 802, 2, 24, 45, 75, 66]\nTens digit: [802, 2, 24, 45, 55, 66, 170, 90]\nHundreds:   [2, 24, 45, 66, 75, 90, 170, 802]\n```\n\n**Complexity**: O(d × (n + k)) where d = number of digits, k = radix (10 for decimal).\n\n## Comparison\n\n| Algorithm | Time | Space | Constraints |\n|-----------|------|-------|------------|\n| Counting Sort | O(n + k) | O(k) | Integer range limited |\n| Radix Sort | O(d(n + k)) | O(n + k) | Fixed-base keys |\n| Quick Sort | O(n log n) avg | O(log n) | None (general) |\n\n## When to Use Non-Comparison Sorts\n\n1. **Counting Sort**: Small integer range relative to n (e.g., sorting 1M numbers in range 0-1000)\n2. **Radix Sort**: Fixed-length integer or string keys (e.g., sorting dates, phone numbers)\n3. Both are great when the inputs have known, limited structure\n\n## Limitations\n\n- Counting Sort: wasteful if range k >> n\n- Radix Sort: overhead per digit can make it slower than O(n log n) for many inputs\n- Neither works for floating-point or arbitrary objects without transformation',
    code_example: '#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nvoid countingSort(int arr[], int n, int exp) {\n    vector<int> count(10, 0), output(n);\n    for (int i = 0; i < n; i++) count[(arr[i] / exp) % 10]++;\n    for (int i = 1; i < 10; i++) count[i] += count[i - 1];\n    for (int i = n - 1; i >= 0; i--) {\n        output[count[(arr[i] / exp) % 10] - 1] = arr[i];\n        count[(arr[i] / exp) % 10]--;\n    }\n    for (int i = 0; i < n; i++) arr[i] = output[i];\n}\n\nvoid radixSort(int arr[], int n) {\n    int maxVal = *max_element(arr, arr + n);\n    for (int exp = 1; maxVal / exp > 0; exp *= 10)\n        countingSort(arr, n, exp);\n}\n\nvoid printArray(int arr[], int n) {\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n    cout << endl;\n}\n\nint main() {\n    int arr[] = {170, 45, 75, 90, 802, 24, 2, 66};\n    int n = sizeof(arr)/sizeof(arr[0]);\n    cout << \"Original: \"; printArray(arr, n);\n    radixSort(arr, n);\n    cout << \"Sorted:   \"; printArray(arr, n);\n    return 0;\n}'
  }
];
export default sortingLessons;
